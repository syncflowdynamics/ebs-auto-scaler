[general]
interval = {{ interval }}
threshold = {{ threshold }}
increase_type = {{ increase_type }}
increase_gb = {{ increase_gb }}

[notification]
enabled = {{ notification.enabled | string | lower }}
email-sender = {{ notification['email-sender'] }}
email-recipients = {{ notification['email-recipients'] | join(',') }}

[exclude]
volumes = {{ exclude.volumes | join(',') }}